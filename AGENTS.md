Portable NeoVim (nightly) configuration.

Goals:
* Use the (`>v0.12`) `vim.pack` functionality for package management.
* The config must fully work:
  * by simply cloning it into `~/.config/nvim`
  * via `nix run` (e.g. `nix run github:my-username/this-repo`)
